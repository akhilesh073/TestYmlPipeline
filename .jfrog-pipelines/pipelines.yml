resources:
  - name: self_repo
    type: GitRepo
    configuration:
      gitProvider: testInt
      path: akhilesh073/TestYmlPipeline
      branches:
        include: ^tests$

pipelines:
  - name: very_long_long_long_name_long_name_name_long_name
    steps:
      - name: Normal_Step
        type: Bash
        configuration:
          inputResources:
            - name: self_repo
        execution:
          onExecute:
            - pushd "$res_self_repo_resourcePath/"
            - save_tests saveTests/failing
            - echo "Passed s"
            - popd
